<UserControl
    x:Name="_root"
    x:Class="TreeSimulation.NamedField"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TreeSimulation"
    xmlns:c="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="70"
    d:DesignWidth="350">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <TextBlock 
            Text="{Binding ElementName=_root, Path=Title}" 
            TextAlignment="Center" 
            VerticalAlignment="Center" />
        <c:NumberBox 
            Grid.Column="1" 
            Minimum="{Binding ElementName=_root, Path=MinValue}" 
            Maximum="{Binding ElementName=_root, Path=MaxValue}"
            Value="{Binding ElementName=_root, Path=Value}" 
            SpinButtonPlacementMode="Compact" 
            VerticalAlignment="Center" 
            HorizontalAlignment="Stretch" 
            Margin="10 0" />
    </Grid>
</UserControl>
